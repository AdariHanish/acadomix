import { useEffect } from 'react';

/**
 * Disabled: This was originally added to fix hover states sticking on scroll,
 * but setting pointer-events: none causes scroll wheel issues on some devices.
 */
export function useScrollHoverFix() {
  // Logic removed to prevent scrolling from getting stuck
}
